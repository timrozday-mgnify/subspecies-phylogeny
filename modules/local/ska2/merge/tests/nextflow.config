process {
    withName: 'SKA2_MERGE' {
        // Forces the multi-batch fold path in the "three genomes" test below with
        // only 3 fixtures, instead of needing >100 real genomes to exercise it.
        ext.merge_batch_size = 2
    }
}
